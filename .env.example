OPENAI_API_KEY="sk-your-openai-api-key-here"

# Optional: Enforce client key from Claude Code
ANTHROPIC_API_KEY="your-expected-anthropic-api-key"

# Optional: Upstream OpenAI-compatible endpoint
OPENAI_BASE_URL="https://api.openai.com/v1"
# AZURE_API_VERSION="2024-03-01-preview"

# Model mapping
BIG_MODEL="gpt-4o"
MIDDLE_MODEL="gpt-4o"
SMALL_MODEL="gpt-4o-mini"

# Server settings
HOST="0.0.0.0"
PORT="8082"
LOG_LEVEL="INFO"

# Request constraints
MAX_TOKENS_LIMIT="4096"
MIN_TOKENS_LIMIT="100"
REQUEST_TIMEOUT="90"
# Max allowed inbound JSON body size in bytes (default in app: 16MB)
REQUEST_BODY_MAX_SIZE="16777216"

# Optional custom headers for upstream requests
# CUSTOM_HEADER_ACCEPT="application/json"
# CUSTOM_HEADER_X_API_KEY="your-provider-api-key"
